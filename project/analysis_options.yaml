include: package:flutter_lints/flutter.yaml

# lib/spike_rtmp/ (the v0.7 Checkpoint 1 RTMP spike) was deleted in P2: it was
# superseded by lib/features/live_stream/services/rtmp_publish_engine.dart,
# never built, and excluded from analysis. Its history is in git.
