name: streamer_app
description: "Educational Cloud Streaming Application for Saudi Arabia (AlSharqia / KSA)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Routing & State Architecture
  go_router: ^14.2.0
  provider: ^6.1.2
  http: ^1.2.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.3.0

  # Localization (English LTR & Arabic RTL)
  easy_localization: ^3.0.7

  # GIS Mapping & Vector Rendering
  flutter_map: ^7.0.2
  flutter_map_cancellable_tile_provider: ^3.0.1
  cached_network_image: ^3.3.1
  latlong2: ^0.9.1
  flutter_svg: ^2.0.9
  vector_math: ^2.1.4

  # Media Streaming & Video Viewports
  youtube_player_iframe: ^5.2.1
  wakelock_plus: ^1.2.8
  webview_flutter: ^4.8.0
  webview_flutter_android: ^4.12.0
  webview_flutter_wkwebview: ^3.25.1

  # Design System & UI Typography
  google_fonts: ^6.2.1
  top_snackbar_flutter: ^3.1.0

  # Media Picker
  image_picker: ^1.1.2

  # Native OS share sheet (Private Streaming invite link)
  share_plus: ^10.1.4

  # Phone Camera/Mic Broadcasting (v0.7) -- runtime permission requests for
  # CAMERA/RECORD_AUDIO ahead of the native RootEncoder RTMP publisher.
  permission_handler: ^11.3.1

  # Backend
  supabase_flutter: ^2.17.2
  uuid: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/map/gadm41_SAU_2.svg
    - assets/Language.svg
    - assets/i18n/en.json
    - assets/i18n/ar.json
    - assets/images/Ahmed_Amer_YouTube_files/
    - assets/images/Amir_Alhatemi/
    - assets/images/BidonWaraq/
    - assets/images/Dalilak/
    - assets/images/quran/
