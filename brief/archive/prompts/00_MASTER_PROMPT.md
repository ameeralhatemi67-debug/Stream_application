# 00 — MASTER PROMPT for Claude Code

**Owner checklist (2 minutes, before pasting).** Using Codex/Astra instead of Claude Code? Use `00_MASTER_PROMPT_ASTRA.md` and the set-up in `04_BUDGET_PROTOCOL.md` §G. Want the new design (colours, gradients, fonts, logo)? Run `DESIGN_PROMPT.md` first (Astra is fine for it), pick A/B/C, set `DESIGN_CHOICE` in 05.
1. Fill the blanks in `brief/05_DECISIONS.md` → "Owner inputs" (app id, names, support email, privacy URL). Blank = Claude Code will not rename anything and will leave exact rename steps for you.
2. From the repo root run `claude --permission-mode acceptEdits --settings brief/claude_settings.json` (loads the usage meter and the safety rules). Send one throw-away message ("hi"), and check the status line shows `5h nn% (reset nnm) | 7d nn% | cache 1h warm`. If it says `5h n/a`, stop: see `brief/04_BUDGET_PROTOCOL.md` §A.
3. Paste everything between the two marker lines when about **60 minutes** remain in your 5-hour window, and keep the terminal open. Don't switch model/effort afterwards. Change `PLAN:` only if you want to force SINGLE or SPLIT.
4. If the run stops early, it leaves `brief/archive/reports/2026-09-22/OVERRUN_REPORT.md` (why, with numbers). Continue later with `brief/archive/prompts/RESUME_PROMPT.md`.

--- paste from here ---
PLAN: AUTO   (AUTO | SINGLE | SPLIT — rules in brief/04_BUDGET_PROTOCOL.md §B)
OWNER_INPUTS: brief/05_DECISIONS.md, block "Owner inputs". Blank = do not guess, do not rename.

You are Claude Code, working autonomously on this repo: Streamer_app, a Flutter + Supabase app (Arabic/English, RTL) for live educational lectures in Saudi Arabia, shipping on Android. The owner is away and will not answer questions. If something is undecided, use `brief/05_DECISIONS.md`; if it is not there, choose the safest option, log one line in the ledger, and continue.

## 1. BUDGET — a hard limit that outranks finishing the work
- The meter is the account-wide 5-hour usage percentage, read with `node brief/tools/budget_check.mjs --plan <plan>`. Absolute caps: SINGLE 80 (soft 74) · SPLIT 70 (soft 64) then 60 (soft 54) across two 5-hour windows · never a third window · weekly ≥ 90 ⇒ stop. Full rules: `brief/04_BUDGET_PROTOCOL.md`. Read it now, first, before anything else.
- Your FIRST actions are 04 "Step 0": read the meter, choose the plan (AUTO rule), fill in the run header of `brief/LEDGER.md` (a template). If the meter cannot be read, STOP immediately and tell the owner to do 04 §A. Never proceed on an assumed number and never claim OK without a fresh meter line.
- Check the meter at every phase start, every 10-15 tool calls, after any command over 2 minutes, and before any build or full test run. Obey the status: OK ⇒ continue; SOFT ⇒ finish only the step in progress, run the phase checkpoint, start nothing new; STOP ⇒ start nothing, put the tree in a safe state; UNKNOWN ⇒ do what the output says.
- If you cannot finish inside the cap, stop at a safe place (04 §E) and write `brief/archive/reports/2026-09-22/OVERRUN_REPORT.md` explaining WHY, with numbers: stop reason, plan and windows used, estimate vs actual per phase, the 3 biggest consumers and their cause, what is left, and how to resume. Stopping early and clean beats finishing half-done. Never run past a STOP to "finish quickly", never edit the caps/tools/`.runtime`, and never use the reserve (the remaining 20 %/30 % belongs to the owner's other work).
- SPLIT is allowed only with a warm 1-hour cache. Cross the window reset with the nap loop in 04 §D so the cache stays warm; do nothing else while waiting.
- Session hygiene (cache): no model, effort, plugin or MCP change; no manual `/compact`; no subagents (at most one read-only Explore for a repo-wide question the map cannot answer).

## 2. Context — read exactly these, then stop exploring
`brief/04_BUDGET_PROTOCOL.md`, `brief/01_PROJECT_MAP.md`, `brief/02_AUDIT_DELTA.md`, `brief/03_WORK_PLAN.md`, `brief/05_DECISIONS.md`; `brief/06_VERIFICATION.md` when you first verify. They hold a static audit with file:line anchors so you do not spend budget rediscovering the project. Nothing was ever built or run before you: the anchors and every item marked VERIFY are unproven until you check them. Grep an anchor before editing (lines drift); if it is wrong, note it in the ledger and fix the line there — do not go exploring. Other docs (`doc/`, `Roadmap.md`, `STATUS.md`, `To_fix_log.md`) are stale where they disagree with 02.

## 3. The goal — the owner's 22 requests (verdicts and "done means" are in 02; order in 03)
Remove all placeholders/fixtures/simulations · offline map · visuals when offline · improve the map · no overflowed pixel on any screen size · better chat, moderation and admin connection · flesh out organizations · remove unnecessary emojis · better settings layout · fully clean white theme · better UI/responsiveness · true viewer count · confirm Saudi-rule compliance · Android store readiness · viewers, streamers and organizations can reach only what they should (server-enforced, not just hidden UI) · admin has full moderation power and tools · improve security · solid multi-device logic for one user · RLS on with clear, minimal policies · the service key never leaks · apply the owner's chosen design scheme: colours, fonts, logo, and gradients used sparingly as accents (only if `DESIGN_CHOICE` is set) · make no mistakes · stay in budget.
Two honest limits: you cannot certify legal compliance, so you produce the evidence/checklist package and a counsel question list and say plainly it is not certified (03 P8B); and anything you could not run is labelled UNVERIFIED, never "done".

## 4. How to work
- **Order and scope**: 03 as written (P0 → P1 → P2 → P3 → P4 → P8A → P6 → P5 → P7 → P8B → P9) and its cut list when budget is short. Never cut P0-P3 or P9. Percentages in 03 are guesses: after each phase record the real burn and re-plan from your own numbers (also after P1 and P4 re-estimate everything left).
- **Server first**: security and data-truth fixes (P1-P3) come before any UI work. New migrations only (never edit old ones); local Supabase only; never touch a linked or production project (05 D-14). No Docker ⇒ write migrations and the DB probe files anyway and mark the results UNVERIFIED-STATIC.
- **Git**: tag `pre-hardening-2026-09-19` first; commit locally on the current branch, one commit per phase (more if a phase is large); NEVER push. Stage files by explicit path, never `git add -A`/`.`; never stage `.serena/`, `.claude/`, `.codex/`, `_to_delete/`, `brief/.runtime/`, `dart_define.local.json` or `key.properties`; never print `dart_define.local.json` values; never generate or commit a keystore. Use `git rm`/`git mv` for deletions and moves. Commit `brief/LEDGER.md` with each phase.
- **Checkpoint after every phase** (in this order): `flutter analyze` (from `project/`) = the recorded baseline or better; `flutter test --reporter compact 2>&1 | tail -40`, green; `node brief/tools/gates.mjs`; budget meter; commit; ledger update (Δ used_5h, decisions, evidence, NOT DONE) and rewrite the RESUME block, ≤15 lines in total. Between checkpoints re-run only the failing test file, not the suite.
- **Evidence**: 06 §0 tiers. A security, RLS, layout, theme or placeholder claim needs E2 (gates, layout sweep, DB probes). Never write "fixed" above the tier you reached. Do not delete or weaken a test or a gate to get green (05 D-17). Do not touch the ADR-006 YouTube embed/referrer behaviour, and do not delete the real "custom placeholder cards" feature (01 invariants).
- **Token hygiene**: prefer `graft ask "<task>"`, `graft callers <symbol>`, `graft grep <regex>` (CLI, auto-syncs; `graft build` if the graph is missing; check `graft --version` once, fall back to Grep) over opening files. Grep and line-range Reads only. Never read whole any file over ~1,000 lines (`app_provider.dart`, `admin_database_service.dart`, `settings_screen.dart`, `org_management_view.dart`, `spatial_map_screen.dart`, `admin_hub_screen.dart`, …). Do mechanical renames and bulk edits with a small Node script in `brief/tools/` rather than file by file. Don't re-read what you just wrote, don't dump long output (`| tail`, `| head`), don't narrate.
- **Design** (05 D-20, D-25; 03 P4.0): the full white recolor comes from the chosen tokens file only. Gradients only through the `AppGradients` tokens at the few allowed places, never ad hoc (gate G2d = 0); text on a gradient passes contrast on both stops.
- **RLS and secrets** (05 D-21, D-22; 03 P1.10-1.11): only the anon key belongs in the app; the service-role key, `sb_secret_*`, DB passwords and keystores never appear in any file, log, ledger, test or chat output; gates G11 stay 0 and the release AAB must pass `node brief/tools/scan_build_secrets.mjs <aab>`. A key found in git history is an OWNER_ACTIONS "rotate now" item, never a history rewrite.
- **Owner-only things** (keystore, Play Console, real Supabase push, legal review, key restrictions, …): never fake them. Append the exact commands/values to `brief/OWNER_ACTIONS.md` and continue.
- **When in doubt about a destructive step** (deleting a file family, a schema change touching existing data): grep for usage first, prefer hiding behind a `false` const over deleting if usage is unclear, and record why.

## 5. Finishing
Stop at a safe state (04 §E: last commit complete, tree clean or a named stash, analyze/tests at baseline or better, no half-written migration, ledger + RESUME block current). Then write `brief/FINAL_REPORT.md` (everything done) or `brief/archive/reports/2026-09-22/OVERRUN_REPORT.md` (budget stop), each ≤ 1 page, honest about what is verified vs unverified. Your last chat message is at most 12 lines and points to that report.

Begin: 04 Step 0.
--- end ---
