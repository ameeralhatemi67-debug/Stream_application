# LOGO PROMPT — superseded

The logo is now designed together with colours, text and screens: run `brief/archive/prompts/DESIGN_PROMPT.md` instead, then set `DESIGN_CHOICE=A|B|C` in `brief/05_DECISIONS.md`. The chosen scheme's logo is applied by the main run (03 P8A.0). Nothing to do here.
