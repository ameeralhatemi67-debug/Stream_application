# 00 — MASTER PROMPT for Codex (GPT-6 Astra)

Slim on purpose: OpenAI's guidance for Astra is outcome and stopping rules instead of long step lists, and pointers instead of "read everything first". The heavy detail lives in `brief/`; the agent opens it when a step needs it. Codex reads `AGENTS.md` by itself, so nothing from it is repeated here.

**Owner checklist (5 minutes):** 1) `brief/05_DECISIONS.md` inputs are filled (`DESIGN_CHOICE` after `brief/DESIGN_PROMPT.md`, if you want the new colours, gradients, fonts and logo applied). 2) Do `brief/04_BUDGET_PROTOCOL.md` §G set-up (status-line items, rules file, `--probe` must pass). 3) Model Astra, effort **medium**, fast mode **off**, start from the repo root, keep them unchanged for the whole session. 4) Paste the block below. On Codex there is no cache reason to wait for "one hour left": paste whenever you like; if only an hour of window 1 remains, the run simply spends what is left of window 1 up to its cap (70, soft stop 64). 5) At the window-1 stop it writes a RESUME block and quits. After the window resets (Codex resets on your first message after it ends) open a **fresh** session and paste `brief/RESUME_PROMPT.md` with `MODE: CONTINUE_SPLIT` (window 2, cap 60).

--- paste from here ---
PLAN: split   (split = window 1 cap 70, window 2 cap 60 | single = one window, cap 80)

Autonomous run, no one will answer questions: use `brief/05_DECISIONS.md` for any decision, log one line in `brief/LEDGER.md`, continue.

**Outcome.** This repo is a Flutter + Supabase app (Hadayah Live, Arabic/English RTL, Android) for live educational lectures in Saudi Arabia. Bring it to release-ready by delivering the owner's request list (02 rows 1-26): remove placeholders and simulations, offline map and offline visuals, better map, no overflow on any screen size, better chat/moderation/admin link, fleshed-out organizations, no needless emoji, better settings layout, clean white theme from the chosen design scheme (gradients only as sparse accents via tokens, 05 D-25), better UI and responsiveness, true viewer count, Saudi-compliance evidence package, Android store readiness, server-enforced access limits for viewers/streamers/organizations, full admin moderation tools, RLS on with clear policies, no service key leak, better security, solid multi-device logic, no mistakes, and stay inside the budget. A static audit with file:line anchors is in `brief/`: map (01), verdicts and "done means" (02), phase plan and cut list (03), decisions (05), verification playbook (06). Start with 03's current phase and 05; open the others when a step needs them. Grep an anchor before editing; if a line drifted, fix the note, don't explore.

**Budget: a hard limit that outranks finishing.** Use the PLAN value above (`<plan>` below). `split`: two windows, never a third: window 1 cap 70 % of the 5-hour meter (soft stop 64), window 2 cap 60 % (soft 54). `single`: one window, cap 80 (soft 74). Weekly ≥ 90 stops everything. The caps are absolute account-wide numbers, so whatever the owner already spent in the window counts. First action: `node brief/tools/budget_check.mjs --plan <plan> --new-run` (`--new-run` only this once), then fill the ledger header and read `brief/04_BUDGET_PROTOCOL.md` §B, §C, §E and §G once. Afterwards run `node brief/tools/budget_check.mjs --plan <plan>` at each phase start, every 10-15 tool calls and before a build or full test run, and obey its status (OK continue; SOFT finish the current step only; STOP or UNKNOWN start nothing and reach a safe state). At a window-1 SOFT or STOP under `split`: reach a safe state, write the RESUME block in the ledger, and stop; do not wait, sleep or nap, the owner starts a fresh session in the next window. If you cannot finish inside the caps, stop at a safe place and write `brief/OVERRUN_REPORT.md` saying why, with numbers (per-phase estimate vs actual, the three biggest consumers, what is left, how to resume). Never edit the meter tools or caps, never run past a STOP. If two consecutive steps each move the meter more than 25 points, stop and say the plan is too small.

**Hard rules.** Never `git push`. Local Supabase only; never link, push, or use a project ref; migrations are new files. Never print, store or commit secrets: only the anon key belongs in the app, and the service-role key never appears anywhere (05 D-22; gates G11 must be 0; scan the release AAB with `scan_build_secrets.mjs`). Stage files by explicit path; never `.serena/`, `.codex/`, `.claude/`, `_to_delete/`, `brief/.runtime/`, `dart_define.local.json`, `key.properties`. Never generate a keystore. Owner-only things (keys, Play Console, real Supabase push, legal review) go to `brief/OWNER_ACTIONS.md`, never faked. No subagents. Do not touch the ADR-006 YouTube embed behaviour or the real "custom placeholder cards" feature.

**Working style.** Server-side security and data truth first (03 order P0, P1, P2, P3, P4, P8A, P6, P5, P7, P8B, P9; cut from the bottom of 03's cut list when short, never P0-P3 or P9). Use `graft ask`, `graft callers`, `graft grep` and line-range reads instead of opening large files (never read whole any file over about 1,000 lines). Do bulk edits with a small script in `brief/tools/`. One local commit per phase after `flutter analyze`, `flutter test --reporter compact 2>&1 | tail -40`, and `node brief/tools/gates.mjs`; then update the ledger and its RESUME block. Claims of "fixed" need the evidence tier in 06 §0; what you could not run is labelled UNVERIFIED, never "done". Keep tool output short.

**Done means.** Safe state (last commit complete, tree clean or a named stash, analyze and tests at baseline or better, no half-written migration, ledger and RESUME block current), then `brief/FINAL_REPORT.md` (everything done) or `brief/OVERRUN_REPORT.md` (budget stop), each one page and honest about verified vs unverified. Final message: at most 12 lines pointing to it.

Begin with the meter.
--- end ---
