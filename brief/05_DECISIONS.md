# 05 — Decisions, defaults and owner inputs

Use these defaults without asking. If you must decide something not listed, log it in the ledger (one line: decision + why) and continue.

## Owner inputs — the owner fills these before the run. **Blank = do not guess, do not rename.**
```
APP_ID=sa.hadayah.streamer_app
APP_NAME_EN=Hadayah Live
APP_NAME_AR=منصة هدايه
SUPPORT_EMAIL=ameeralhatemi67@gmail.com
PRIVACY_POLICY_URL=
ICON=brief/assets/icon.png    # optional 1024x1024 PNG; if missing, skip icon generation and list it in OWNER_ACTIONS
DESIGN_CHOICE=                # A | B | C, set after brief/DESIGN_PROMPT.md produced brief/assets/design_options/. Blank = conservative white theme (03 4.1), no gradients, icon/logo untouched (list in OWNER_ACTIONS)
```
Use every value exactly as typed (including the Arabic spelling); never "correct" it. Put them in ONE file, `project/lib/core/config/app_identity.dart` (name en/ar, support email, privacy URL), and read them from there everywhere.
Blank identity ⇒ keep the working title, centralise the name/ids in one place, and put the exact rename procedure in `OWNER_ACTIONS.md`.

## Defaults
| ID | Decision | Default |
|---|---|---|
| D-03 | Simulated features (02 row 1) | **Delete or hide behind a const flag that is `false` in release**: Q&A, venue RSVP/attendance, raise-hand, reminders, multi-speaker *timed* overlays (a static real speaker list may stay), ghost/demo chat, notification simulator/pitch toggles (debug-only), mini-player fake card, `YouTubeLiveService` fake ids/keys + `startQuickPhoneBroadcast`, local-network RTMP/VLC/IVS. **Slides**: only when a real URL exists; "Download" opens it externally. **Notification center**: keep UI with real in-app events only (persistence/push = next epic X1). **VOD/playlists**: real API data or an empty state. **Platform analytics**: real aggregates only, else "No data yet". **Play/pause/mute**: wire to the iframe controller. |
| D-04 | Basemap | Spike self-hosted PMTiles (OSM data, ODbL) first; fallback = ToS-permitted raster + browse-cache + bundled Natural Earth regions. No bulk-downloading from OSM/Esri servers. Attribution always visible. Light style. |
| D-05 | Emoji | Keep only the live reaction glyphs (heart, clap, raised hand, fire, bulb, graduation cap) in the reaction overlay files listed in `brief/emoji_allowlist.txt`. Everything else = Material icons. |
| D-06 | Text scale | Clamp to 1.3 at the app root; layouts must still not overflow at 2.0 in the sweep. |
| D-07 | Follows / bookmarks | Persist for signed-in users (`follows`, `bookmarks` tables, own-row RLS); guests keep local-only state. |
| D-08 | Viewer counting | Heartbeat 20 s, window 45 s, key = user id if signed in else install UUID; unknown shows "—". |
| D-09 | Chat blocks | Server-side own-row table + local cache. |
| D-10 | Age / minors | No age-gate code; add to `store/counsel_questions.md`; don't invent legal text. |
| D-11 | Private streaming | Off (`kPrivateStreamingEnabled=false`) until server-enforced entitlements exist. |
| D-12 | Session hygiene | No model/effort/plugin/MCP change mid-run; no manual `/compact` (if auto-compact happens, re-read only the ledger RESUME block + current phase). |
| D-13 | Git | Commit locally on the current branch (project convention: `master`), one commit per phase, no push, tag `pre-hardening-2026-09-19` first. Never stage `.serena/`, `.claude/`, `brief/.runtime/`, `dart_define.local.json`, `key.properties`. Use `git rm`/`git mv` for deletions/moves. |
| D-14 | Supabase | **Local stack only.** Never run `db push`, `link`, `db reset --linked`, `secrets`, `functions deploy`, or anything that uses a project ref / service-role key; never print `dart_define.local.json` values. Deliver migrations as files; put the review + push commands in OWNER_ACTIONS. No Docker ⇒ static only, `UNVERIFIED-STATIC`. |
| D-15 | Subagents | None (each has its own 5-minute cache and prefix). One read-only `Explore` allowed for a repo-wide question the map/graft can't answer. |
| D-16 | Arabic | Real Arabic (not transliteration), match terminology already in `ar.json`; keep key symmetry. |
| D-17 | Tests | Never delete a test to get green; moved fixtures keep coverage; changed behaviour ⇒ update the test with intent. |
| D-18 | Map region | Eastern Province default view; Saudi-wide bounds and city presets. |
| D-19 | Scripts | Put mechanical-edit scripts in `brief/tools/` (pre-approved by the permission rules). |
| D-20 | Design scheme (colours, text, logo) | Only when `DESIGN_CHOICE` is set: implement exactly that scheme from `brief/assets/design_options/tokens_<choice>.json` (read only that file; the preview screens are the visual reference): one light `ThemeData`, semantic colour tokens, `AppGradients`, typography with the chosen Arabic + Latin fonts **bundled as assets** (licence files kept, no runtime font download, no `google_fonts` fetching: offline use), radii/spacing/shadows, and the scheme's logo/icon (03 P8A.0). Never invent a different palette or logo during the main run. Blank ⇒ 03 4.1 fallback (neutral white theme from the current accent with AA-safe text colours), no gradients. |
| D-21 | RLS / policies | Every table in `public` (and `storage.objects`) has RLS enabled and explicit, minimal, named policies: `to authenticated` (or `to anon` only for the two viewer-count RPCs and deliberate public views), separate policy per command, `(select auth.uid())` form, no `using (true)` on writes, no `for all`. Deny-all tables (RLS on, no policy) carry a `comment on table` saying why. Views are `security_invoker = true` unless a comment justifies otherwise. Functions: `security definer` only with `set search_path = ''` (or `public, pg_temp`), `revoke execute … from public, anon` then grant back only what is needed. Realtime publication lists only intended tables. Prove it with 06 §2b (R-probes) + gates G10; static-only ⇒ UNVERIFIED-STATIC. |
| D-22 | Secrets / service key | The app only ever holds the **anon** key (via `--dart-define-from-file`, gitignored). The service-role key, `sb_secret_*`, DB passwords and keystores never enter the repo, chat output, logs, ledger, docs, tests or a build. Local tests use keys read from `npx supabase status -o env` at run time, never pasted into a file. Add `.env`, `.env.*`, `supabase/.env`, `**/dart_define.local.json`, `*.jks`, `*.keystore`, `key.properties` to `.gitignore` (keep `*.example`). Gates G11a-g must be 0; scan the release AAB with `node brief/tools/scan_build_secrets.mjs <aab>` (P8A, P9). If any key is ever found in git history or a build: do NOT try to rewrite history; record it in OWNER_ACTIONS as "rotate now". |
| D-23 | Harness / model | The run may happen in Claude Code (Opus 5 default) or Codex (GPT-6 Astra). The meter tool auto-detects; Codex specifics are in 04 §G. Pick model + effort once per session and keep them (Claude: `high`; Astra: `medium`, fast mode OFF). A **new session** (after a window ends) may use a different model: phases P4 (mechanical rename/i18n/emoji) and P8B docs are good candidates for a cheaper model/lower effort; P1, P3, P6 (RLS, RPCs, migrations) deserve the strongest. |
| D-24 | Code navigation | Prefer `graft ask "<task>"`, `graft callers <symbol>`, `graft grep <regex>` (CLI, auto-syncs with edits; `graft build` if the graph is missing) over opening files; check `graft --version` once, fall back to Grep if absent. No new MCP servers, plugins or skills during a run (each adds tokens to every turn and changes the cache). |
| D-25 | Gradients | Gradients are an accent on a white app, not a texture. Only through named tokens (`AppGradients.brand/soft/live`, defined once in the theme; no gradient literal in a screen, gate G2d = 0). Two stops (three at most) from one hue family, one angle everywhere: top-start to bottom-end (`AlignmentDirectional`, mirrors in RTL), like `brief/assets/gradient_reference.png`. Allowed only on: the primary button fill (one per screen), logo/icon tile, welcome hero, live badge/ring, small accents (progress, empty-state art). Never on app bars, navigation bars, list cards, dialogs, inputs, borders or body backgrounds. At most two gradient surfaces visible per screen. Text on a gradient must reach 4.5:1 against BOTH stops (theme contrast test), else use a flat colour. No gradient text, no animated gradients, disabled = flat grey. |
| D-26 | Owner cap for a window | The owner may raise the current window's cap once through `OWNER_CAP=N` in the paste (max 90, soft N-6; 04 §I). 2026-09-20: `OWNER_CAP=90` for split window 2. The agent never picks or raises a cap; never a third window; weekly >= 90 still stops. |
| D-27 | Design scope | Owner decision 2026-09-20: the redesign (white theme, colours, gradients, fonts, logo, icon; phases P4 and P8A) is reserved for Astra, after the owner picks `DESIGN_CHOICE`. Claude Code (Opus) sessions skip P4, P8A and P9 and never touch the theme, tokens, fonts, logo or icons. |

## Owner-only actions (append the specifics to `brief/OWNER_ACTIONS.md`; never fake them)
Generate the upload keystore + `android/key.properties` · Play Console account, developer verification, Play App Signing · review then push migrations to the real Supabase (`npx supabase db push` after reading the diff) · update Supabase Auth Redirect URLs/Site URL for the new deep-link scheme · decide Supabase region (PDPL cross-border) · restrict the YouTube API key (Android package + SHA-1s of upload *and* Play signing key + API restriction) · Google OAuth consent-screen branding/verification for the new app name · host public privacy-policy and account-deletion pages · lawyer review of CST/GCAM licensing, PDPL texts, minors, Arabic ToS · store listing assets, screenshots, content-rating questionnaire, Data-safety form, foreground-service declaration + demo video · choose master-admin accounts and consider removing personal emails from `20260822090000_admin_role_bootstrap.sql` before the repo is ever shared.
